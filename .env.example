# For local development, point to backend server
VITE_API_URL=http://localhost:3000

# For production (Vercel), leave empty or use relative path
# The API will be available at /api/* on the same domain
